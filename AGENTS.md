# AGENTS.md — P007 dsh-attention-pack

本目录是 DSH 客户端插件「注意力套装」的独立 git 仓库（对外开源目标）。

- 上级护栏：`~/AGENTS.md`（三机统一安全护栏）与 `~/workspace/` 总规约照常生效。
- 工程准则：最小改动；每步带可验证目标；说"好了"之前必须亲眼见过（DOM 验证 + 老板肉眼确认）。
- 红线：不碰 `~/.hermes/secrets/*`；不 pkill -f；对外发布前先确认。
- 踩坑记 `PITFALLS.md`，进度记 `OVERVIEW.md`。
